package ch02;

public class OperatorPriority {
    public static void main(String[] args) {

                // 연산자 우선순위
                // 단항연산자(++, --, +/-, !)
                // 산술연산자(+, -, *, /, %)
                // 크기비교연산자(>, >=, <, <=)
                // 동치비교연산자(==, !=)
                // 논리연산자(&&, ||)
                // 3항 ( ? : )
                // 대입연산자(0+=, -+, *=, /=, %=)
                int number = 10;
                // number = number + 10
                number += 10;
                number -= 10;
                // number = number + 10}
            }
        }

